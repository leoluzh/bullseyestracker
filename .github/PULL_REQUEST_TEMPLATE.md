## What changed and why

<!-- Not just what — why. Link the relevant spec/tasks.md item if this implements one. -->

## Testing

- [ ] `make build` passes (compile + unit tests + lint)
- [ ] `make connected-test` run on a device, if this touches `cv` detector code
- [ ] Manually verified on-device, if this touches UI/camera behavior

## Known gaps / follow-ups

<!-- Anything unverified, deferred, or intentionally out of scope for this PR. -->

## Checklist

- [ ] `tasks.md` checkboxes updated to match what's actually implemented (if applicable)
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
- [ ] No secrets, credentials, or local machine paths committed (check `local.properties`
      stayed out of the diff)
